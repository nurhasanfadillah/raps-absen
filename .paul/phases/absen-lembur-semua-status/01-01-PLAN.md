---
phase: absen-lembur-semua-status
plan: 01
type: execute
autonomous: true
---

<objective>
## Goal
Pindahkan input jam lembur keluar dari kondisional `status === 'Present'` agar semua status kehadiran bisa mengisi lembur.
</objective>

<context>
@.paul/phases/absen-lembur-semua-status/CONTEXT.md
@pages/Attendance.tsx
</context>

<acceptance_criteria>
## AC-1: Input lembur muncul di semua status
```gherkin
Given modal absensi terbuka
When admin memilih status apa pun (Hadir, Sakit, Izin, Alpa, Libur/Off)
Then field "Input Jam Lembur" selalu tampil
```

## AC-2: Data lembur tersimpan tanpa reset
```gherkin
Given admin mengisi jam lembur = 3 dengan status "Off"
When admin menekan Simpan Data
Then record tersimpan dengan overtimeHours = 3 (bukan 0)
```
</acceptance_criteria>

<tasks>
<task type="auto">
  <name>Pindahkan blok overtime keluar dari kondisional Present</name>
  <files>pages/Attendance.tsx</files>
  <action>
    Ada dua perubahan di file ini:

    1. Di JSX form (sekitar baris 238–289):
       Blok `{modalForm.status === 'Present' && (...)}` berisi: input checkInTime, input checkOutTime, warning boxes, dan blok overtime.
       Pisahkan blok overtime (`<div className="bg-brand-950/30 ...">`) dari dalam kondisional tersebut — pindahkan ke BAWAH penutup `</>` kondisional Present, sehingga tetap berada di dalam form tapi muncul untuk semua status.
       checkInTime, checkOutTime, dan warning boxes TETAP di dalam kondisional Present.

    2. Di fungsi `handleSaveModal` (sekitar baris 67):
       Ubah:
         `overtimeHours: modalForm.status === 'Present' ? Number(modalForm.overtimeHours) : 0,`
       Menjadi:
         `overtimeHours: Number(modalForm.overtimeHours),`

    Jangan ubah logika checkInTime dan checkOutTime yang tetap hanya disimpan saat status Present.
  </action>
  <verify>
    Buka modal absensi, pilih status "Off" → field lembur harus tampil.
    Isi lembur = 2, simpan → buka kembali modal → nilai lembur harus 2, bukan 0.
  </verify>
  <done>AC-1 dan AC-2 terpenuhi: input lembur tampil di semua status dan data tersimpan benar.</done>
</task>
</tasks>

<output>
Setelah selesai, buat `.paul/phases/absen-lembur-semua-status/01-01-SUMMARY.md`
</output>
