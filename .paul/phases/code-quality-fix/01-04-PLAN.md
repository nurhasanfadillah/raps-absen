---
phase: code-quality-fix
plan: 04
type: execute
wave: 4
depends_on: ["01-03"]
files_modified:
  - constants.ts
  - utils/format-date.ts
  - pages/Employees.tsx
  - pages/CashAdvance.tsx
  - pages/Payroll.tsx
  - pages/Dashboard.tsx
  - pages/ActivityLogs.tsx
  - services/pdfService.ts
  - package.json
autonomous: true
---

<objective>
## Goal
Tambah validasi input dengan Zod, centralisasi magic strings ke constants, buat timezone utility, dan implementasikan client-side pagination untuk halaman Employees dan CashAdvance.

## Purpose
Form bisa terima nilai invalid (gaji negatif, dll), magic strings tersebar di seluruh codebase, timezone tidak konsisten (mix UTC dan Asia/Jakarta), dan halaman Employees/CashAdvance tidak punya pagination yang bisa memperlambat UX saat data bertambah.

## Output
- `constants.ts` — semua status values dan action type strings
- `utils/format-date.ts` — formatJakartaTime utility
- `pages/Employees.tsx` — Zod form validation + pagination
- `pages/CashAdvance.tsx` — Zod form validation + pagination
- Input date dengan min/max constraints di Payroll.tsx
</objective>

<context>
@.paul/phases/code-quality-fix/DISCOVERY.md
@.paul/phases/code-quality-fix/01-03-SUMMARY.md
@types.ts
@pages/Employees.tsx
@pages/CashAdvance.tsx
@pages/Payroll.tsx
@pages/Dashboard.tsx
@services/pdfService.ts
</context>

<acceptance_criteria>

## AC-1: Form Tidak Bisa Terima Data Invalid
```gherkin
Given form tambah karyawan terbuka
When user memasukkan gaji negatif atau nama kosong
Then tombol submit menampilkan pesan error Zod sebelum data dikirim ke Supabase
```

## AC-2: Kasbon Form Tervalidasi
```gherkin
Given form tambah kasbon terbuka
When user memasukkan jumlah 0 atau negatif, atau alasan kosong
Then tombol submit menampilkan pesan error validasi
```

## AC-3: Pagination Aktif di Employees dan CashAdvance
```gherkin
Given ada lebih dari 10 data karyawan/kasbon
When user membuka halaman Employees atau CashAdvance
Then data ditampilkan 10 per halaman dengan navigasi prev/next
```

## AC-4: Magic Strings Tergantikan Constants
```gherkin
Given `constants.ts` sudah ada
When membuka file manapun yang menggunakan status attendance
Then menggunakan nilai dari `ATTENDANCE_STATUS` bukan string literal
```

## AC-5: Timezone Konsisten
```gherkin
Given `formatJakartaTime` utility ada
When menampilkan timestamp di ActivityLogs dan Dashboard
Then semua waktu menggunakan Asia/Jakarta timezone secara konsisten
```

</acceptance_criteria>

<tasks>

<task type="auto">
  <name>Task 1: Constants + Timezone utility + Date input constraints</name>
  <files>constants.ts, utils/format-date.ts, pages/Payroll.tsx, pages/ActivityLogs.tsx, pages/Dashboard.tsx, services/pdfService.ts</files>
  <action>
    **A. Buat `constants.ts`:**
    ```ts
    export const ATTENDANCE_STATUS = {
      PRESENT: 'Present',
      SICK: 'Sick',
      PERMISSION: 'Permission',
      ALPHA: 'Alpha',
      OFF: 'Off',
    } as const;

    export const EMPLOYEE_STATUS = {
      ACTIVE: 'Active',
      INACTIVE: 'Inactive',
    } as const;

    export const PAYROLL_STATUS = {
      DRAFT: 'Draft',
      FINAL: 'Final',
      PAID: 'Paid',
    } as const;

    export const ACTION_TYPE = {
      CREATE: 'CREATE',
      UPDATE: 'UPDATE',
      DELETE: 'DELETE',
      SYSTEM: 'SYSTEM',
      AUTH: 'AUTH',
    } as const;
    ```
    CATATAN: Jangan paksa-replace semua penggunaan string literal sekarang — itu bisa jadi pekerjaan besar. Cukup buat file-nya dan ganti di file baru yang kita buat. Halaman yang ada bisa diupdate secara gradual.

    **B. Buat `utils/format-date.ts`:**
    ```ts
    const JAKARTA_TZ = 'Asia/Jakarta';

    export function formatJakartaTime(isoString: string): string {
      return new Intl.DateTimeFormat('id-ID', {
        timeZone: JAKARTA_TZ,
        year: 'numeric',
        month: 'short',
        day: 'numeric',
        hour: '2-digit',
        minute: '2-digit',
      }).format(new Date(isoString));
    }

    export function formatJakartaDate(isoString: string): string {
      return new Intl.DateTimeFormat('id-ID', {
        timeZone: JAKARTA_TZ,
        year: 'numeric',
        month: 'long',
        day: 'numeric',
      }).format(new Date(isoString));
    }
    ```

    **C. Gunakan `formatJakartaTime` di:**
    - `pages/ActivityLogs.tsx` — ganti format timestamp yang ada
    - `pages/Dashboard.tsx` — ganti format timestamp yang ada
    - `services/pdfService.ts` — ganti inline timezone formatting dengan utility

    **D. Date input constraints di `pages/Payroll.tsx`:**
    Pada input date untuk period start dan period end, tambah:
    - `periodEnd` input: `min={periodStart}` — end date tidak bisa sebelum start
    - `periodStart` input: `max={todayDate}` — tidak bisa set payroll di masa depan
    Dimana `todayDate = new Date().toISOString().split('T')[0]`.
  </action>
  <verify>
    - `constants.ts` ada di root project
    - `utils/format-date.ts` ada
    - `pages/ActivityLogs.tsx` menggunakan `formatJakartaTime`
    - Input tanggal di Payroll.tsx punya `min`/`max` attribute
    - `npx tsc --noEmit` — 0 errors
  </verify>
  <done>AC-4 dan AC-5 terpenuhi: constants terdefinisi, timezone konsisten</done>
</task>

<task type="auto">
  <name>Task 2: Zod validation di Employees dan CashAdvance forms</name>
  <files>package.json, pages/Employees.tsx, pages/CashAdvance.tsx</files>
  <action>
    **A. Install Zod:**
    ```
    npm install zod
    ```

    **B. Tambah Zod schema dan validation di `pages/Employees.tsx`:**

    Di atas komponen, definisikan schema:
    ```ts
    import { z } from 'zod';

    const employeeSchema = z.object({
      fullName: z.string().min(3, 'Nama minimal 3 karakter'),
      role: z.enum(['Admin', 'Staff', 'Manager', 'Intern']),
      baseSalary: z.number().positive('Gaji pokok harus lebih dari 0'),
      overtimeRate: z.number().min(0, 'Tarif lembur tidak boleh negatif'),
      joinDate: z.string().min(1, 'Tanggal bergabung wajib diisi'),
      phone: z.string().min(10, 'Nomor HP minimal 10 digit'),
      status: z.enum(['Active', 'Inactive']),
      email: z.string().email('Format email tidak valid').optional().or(z.literal('')),
    });

    type EmployeeFormErrors = Partial<Record<keyof z.infer<typeof employeeSchema>, string>>;
    ```

    Tambah state error di komponen:
    ```ts
    const [formErrors, setFormErrors] = useState<EmployeeFormErrors>({});
    ```

    Di `handleSubmit`, validasi sebelum API call:
    ```ts
    const result = employeeSchema.safeParse({
      ...formData,
      baseSalary: Number(formData.baseSalary),
      overtimeRate: Number(formData.overtimeRate),
    });
    if (!result.success) {
      const errors: EmployeeFormErrors = {};
      result.error.issues.forEach(issue => {
        const field = issue.path[0] as keyof EmployeeFormErrors;
        errors[field] = issue.message;
      });
      setFormErrors(errors);
      return;
    }
    setFormErrors({});
    ```

    Tampilkan error di bawah input yang relevan:
    ```tsx
    {formErrors.fullName && <p className="text-rose-400 text-xs mt-1">{formErrors.fullName}</p>}
    ```

    **C. Tambah Zod schema dan validation di `pages/CashAdvance.tsx`:**
    ```ts
    import { z } from 'zod';

    const cashAdvanceSchema = z.object({
      employeeId: z.string().min(1, 'Pilih karyawan'),
      amount: z.number().positive('Jumlah kasbon harus lebih dari 0'),
      date: z.string().min(1, 'Tanggal wajib diisi'),
      reason: z.string().min(5, 'Alasan minimal 5 karakter'),
    });
    ```

    Terapkan pattern yang sama: `safeParse` di submit handler, tampilkan errors di bawah inputs.

    Hindari: jangan gunakan Zod `parse` (throws) — gunakan `safeParse` (returns result object) agar error bisa ditampilkan tanpa try-catch.
  </action>
  <verify>
    - `zod` ada di `package.json` dependencies
    - Submit form dengan nama kosong → menampilkan error "Nama minimal 3 karakter"
    - Submit form dengan gaji negatif → menampilkan error "Gaji pokok harus lebih dari 0"
    - Submit kasbon dengan jumlah 0 → menampilkan error
    - Submit data valid → berhasil seperti sebelumnya
    - `npx tsc --noEmit` — 0 errors
  </verify>
  <done>AC-1 dan AC-2 terpenuhi: form validasi Zod aktif di Employees dan CashAdvance</done>
</task>

<task type="auto">
  <name>Task 3: Client-side pagination di Employees dan CashAdvance</name>
  <files>pages/Employees.tsx, pages/CashAdvance.tsx</files>
  <action>
    **A. Buat inline pagination hook di masing-masing file** (tidak perlu file terpisah — simpel cukup inline):

    Di `pages/Employees.tsx`, tambah state dan logic pagination:
    ```ts
    const ITEMS_PER_PAGE = 10;
    const [currentPage, setCurrentPage] = useState(1);

    // Hitung filtered list dulu (search sudah ada)
    const filteredEmployees = employees.filter(/* existing search/filter */);

    // Lalu paginate
    const totalPages = Math.ceil(filteredEmployees.length / ITEMS_PER_PAGE);
    const paginatedEmployees = filteredEmployees.slice(
      (currentPage - 1) * ITEMS_PER_PAGE,
      currentPage * ITEMS_PER_PAGE
    );

    // Reset ke halaman 1 saat search berubah
    const handleSearchChange = (value: string) => {
      setSearchTerm(value);
      setCurrentPage(1);
    };
    ```

    Ganti render list dari `filteredEmployees.map(...)` ke `paginatedEmployees.map(...)`.

    Tambah pagination controls di bawah tabel:
    ```tsx
    {totalPages > 1 && (
      <div className="flex items-center justify-between px-6 py-4 border-t border-slate-800">
        <p className="text-sm text-slate-500">
          Menampilkan {(currentPage - 1) * ITEMS_PER_PAGE + 1}–{Math.min(currentPage * ITEMS_PER_PAGE, filteredEmployees.length)} dari {filteredEmployees.length}
        </p>
        <div className="flex gap-2">
          <button
            onClick={() => setCurrentPage(p => Math.max(1, p - 1))}
            disabled={currentPage === 1}
            className="px-3 py-1.5 text-sm bg-slate-800 hover:bg-slate-700 disabled:opacity-40 disabled:cursor-not-allowed text-slate-300 rounded-lg transition-colors"
          >
            Sebelumnya
          </button>
          <span className="px-3 py-1.5 text-sm text-slate-400">
            {currentPage} / {totalPages}
          </span>
          <button
            onClick={() => setCurrentPage(p => Math.min(totalPages, p + 1))}
            disabled={currentPage === totalPages}
            className="px-3 py-1.5 text-sm bg-slate-800 hover:bg-slate-700 disabled:opacity-40 disabled:cursor-not-allowed text-slate-300 rounded-lg transition-colors"
          >
            Selanjutnya
          </button>
        </div>
      </div>
    )}
    ```

    **B. Terapkan pattern yang sama di `pages/CashAdvance.tsx`** dengan `ITEMS_PER_PAGE = 10`.

    Hindari: jangan buat komponen `Pagination` tersendiri — overkill untuk dua halaman saja. Inline logic lebih mudah dibaca dan tidak ada abstraksi prematur.
  </action>
  <verify>
    - Dengan < 10 karyawan: pagination controls tidak muncul (totalPages = 1)
    - Dengan > 10 karyawan (bisa mock di state): pagination controls muncul, klik "Selanjutnya" menampilkan halaman 2
    - Search/filter mereset halaman ke 1
    - `npx tsc --noEmit` — 0 errors
  </verify>
  <done>AC-3 terpenuhi: pagination aktif di Employees dan CashAdvance</done>
</task>

</tasks>

<boundaries>

## DO NOT CHANGE
- `store/` dan semua action files — plan ini hanya di layer UI
- Supabase queries — pagination murni client-side
- `types.ts` di root — jangan ubah interface entitas
- `components/UIComponents.tsx` — jangan modifikasi komponen shared

## SCOPE LIMITS
- Pagination hanya di Employees dan CashAdvance — Attendance sudah per-hari, tidak perlu
- Zod hanya di form input, bukan di data mapping Supabase (itu scope lebih besar)
- Jangan convert semua magic strings ke constants dalam satu plan — hanya file baru yang dibuat
- Tidak ada server-side pagination — murni client filter
</boundaries>

<verification>
Sebelum menyatakan plan selesai:
- [ ] `constants.ts` ada di root
- [ ] `utils/format-date.ts` ada
- [ ] `zod` ada di `package.json` dependencies
- [ ] Form Employees menampilkan error saat submit dengan data invalid
- [ ] Form CashAdvance menampilkan error saat submit dengan jumlah 0
- [ ] Pagination controls muncul saat data > 10 item
- [ ] Input date di Payroll punya `min`/`max`
- [ ] `npx tsc --noEmit` — 0 errors
- [ ] `npm run build` berhasil
</verification>

<success_criteria>
- Zod validation aktif di 2 form utama (Employee + CashAdvance)
- Client-side pagination aktif di 2 halaman list
- constants.ts tersedia sebagai single source of truth untuk status values
- formatJakartaTime digunakan secara konsisten
- Payroll date inputs punya constraints
</success_criteria>

<output>
Setelah selesai, buat `.paul/phases/code-quality-fix/01-04-SUMMARY.md`
</output>
